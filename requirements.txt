fastapi>=0.110.0
uvicorn>=0.30.0
yfinance>=0.2.38
pandas>=2.2,<3.0  # pandas 3.0.5's compiled extension got blocked outright by Windows Application Control on this machine — pin below 3.0 until that's confirmed resolved (see the sibling AlgoTrader project's requirements.txt, where this was hit for real).
numpy>=1.24.0
jinja2>=3.1.0
jugaad-data>=0.30
python-dotenv>=1.0.0
requests>=2.31.0
anthropic>=0.40.0
kiteconnect>=5.0.0
smartapi-python>=1.3.0
pyotp>=2.9.0
websocket-client>=1.6.0

# Optional — only imported when DATABASE_URL is set (see pg_utils.py). Lets a persistent-host
# scanner and a separate stateless deployment (e.g. Vercel) share state via Postgres instead of
# each having its own local files/SQLite the other can't see.
psycopg[binary]>=3.1.0

# Dev/test only — not needed to run the app itself, only `pytest tests/`.
pytest>=8.0.0
httpx>=0.27.0  # required by fastapi.testclient.TestClient
